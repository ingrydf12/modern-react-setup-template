import { createFileRoute } from "@tanstack/react-router";
// change for home / landing 

/* import Home from "@/features/home/components/Home/Home";

export const Route = createFileRoute("/")({
  component: Home,
});
 */